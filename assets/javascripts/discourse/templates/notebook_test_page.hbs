<h1>{{ i18n 'notebook.title' }}</h1>

<form {{ action 'createNote' note on='submit' }}>
  <label>
    {{ i18n 'notebook.create_note.text_field_label' }}
    {{ input name='title' value=note }}
  </label>
  
  <label>
    {{ i18n 'notebook.create_note2.text_field_label' }}
    {{ textarea name='description' value=note44 }}
  </label>

  <button type='submit' class='btn btn-primary'>
    {{ i18n 'notebook.create_note.submit_label' }}
  </button>
</form>

{{#if notes}}
<ul>
  {{#each notes as |note|}}
    <li>
      {{ note.content }}

      <button type="button" class="btn btn-danger" {{ action 'deleteNote' note }}>
        {{ i18n 'notebook.delete_note_label' }}
      </button>
    </li>
  {{/each}}
</ul>
{{/if}}
